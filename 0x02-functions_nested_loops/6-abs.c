#include "main.h"

/**
 * _abs -Computer the absolute value of an integer
 * @t: the integer to be computed
 * Return: The absolute va;ue
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
